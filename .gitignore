# Environment / secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Node / Expo
node_modules/
.expo/
dist/
web-build/
*.tgz

# iOS / Android build artifacts
mobile/ios/
mobile/android/

# ML data
ml/data/images/
ml/data/images.zip
ml/data/images_flagged/
ml/.venv310/

# Model binaries — both tracked in git; force-add after retraining
# mlpackage ~8 MB (iOS CoreML), tflite ~8 MB (Android)

# Misc
.DS_Store
*.log
local.properties
