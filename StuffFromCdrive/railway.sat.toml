[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile.sat"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.watchPatterns]
include = ["backend/**", "railway.sat.toml"]
