[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile.seeder"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.watchPatterns]
include = ["backend/**", "railway.seeder.toml"]
