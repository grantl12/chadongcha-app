[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile.sat"

[deploy]
startCommand = "python workers/satellite_tracker.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

watchPatterns = ["backend/**", "railway.sat.toml"]
