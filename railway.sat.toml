[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile.sat"

[deploy]
startCommand = "python workers/satellite_tracker.py --once"
restartPolicyType = "NEVER"

watchPatterns = ["backend/**", "railway.sat.toml"]
